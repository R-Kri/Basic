"use strict"; // treat all JS code as newer version

// alert(3+3) //We are using nodejs not browser therefore can't be use this alert function for now.

console.log(3+3) // It'll run but (Don't write like this) ,code readability should be high.
let age=18;

/*
DATA TYPES
number => upto 2 to the power 53
bigint (for very very large number of data)
string => ""
boolean => true or false
null => stand alone value (represents empty space)
undefined => value is not assigned yet
symbol => To define uniqueness
object 
*/

// to know the type of the data the variable is storing.
console.log(typeof age);
console.log(typeof undefined); //datatype is undefined
console.log(typeof null); // datatype of null is object